# 轮次收纳插件：两处缺陷修复计划（2026-08-26）

> 状态：缺陷一、缺陷二 2-a、2-b 已实施（2026-08-26）；2-c 按计划留诊断确认。另见 `plugin/docs/code-review-2026-08-26.md` 的残留项。

## Context

`@dsh-plan/turn-collapse`（plugin/）真机出现两个缺陷：

1. **summary 位置错误**：折叠摘要显示在会话**最底部**（落在最终答案与 turn-tail 之后），期望紧贴"它那一轮的最终答案上面"。根因已 100% 确认（锚点被 step/end / llm/retry 事件推越最终答案）。
2. **折叠不一致**：用户实测「完全是正常一问一答也有的没折叠」+「同一回合折叠不完整（部分行折、部分行还在）」。复核后锁定三个根因（见下），其中两个本轮修复、一个留诊断确认。
3. 用户已确认：**分隔线保持现在的低对比即可，不加强**（"对比度说错了是不大，主要是折叠的位置要放对"）。

---

## 缺陷一（位置）——修复 + 夹紧兜底

### 根因

DSH 聊天流渲染顺序完全由节点 `anchorSeq` 升序决定（`@deepseek-ai/dsh-client-ui-conversation/lib/client.js:7988` `sort(a.anchorSeq - b.anchorSeq)`）。事件 seq 单调递增，`step/end` 关闭 step 发生在该步 `assistant/message`（finalize）之后（`dsh-session/lib/types/types.d.ts:245-254`；turn-tail 的 interrupted 推断 `client.js:9192` 佐证）。而 `activity-state.ts` 的 `updateTurnActivityState` 把 **`step/end`、`llm/retry`** 也推进 `lastActivitySeq`，使 summary 锚点 > 最终答案锚点（= 最后 `assistant/message` seq）→ summary 沉底。

### 修复（`plugin/src/client/activity-state.ts`）

- `TurnActivityState` 增加字段 **`lastFinalizeSeq: number | undefined`**；在 `assistant/message` 分支记录 `event.seq`（不推进 `lastActivitySeq`）。
- **`step/end`、`llm/retry` 不再推进 `lastActivitySeq`**（只计 `eventCount`）。保留推进：`assistant/chunk`、`tool/call`、`tool/result`、`step/start`。
- 因此 summary 锚点恒为"最后一个可见活动事件 seq"，严格 ≤ 最后 `assistant/message` seq 之前。

### 夹紧兜底（`plugin/src/client/turn-activity.ts` `buildViewNode`）

即使存在异常事件流（llm/retry 后重流无新 finalize、turn/end 后补发 tool、尾部空 step/start），锚点可能仍 > 最后 finalize seq。加一条 clamp：

```ts
const anchorSeq = Math.min(context.state.lastActivitySeq, context.state.lastFinalizeSeq - 0.1);
```

（`-0.1` 参考 turn-tail 的 `finalizedFollowup` 偏移，DSH 同型；确保 summary 排在任何 `assistant/message` 整数 seq 之前、turn-tail(+0.1) 之后。）`summary` 物化要求 `hasFinalMessage` 为真，故 `lastFinalizeSeq` 必已定义，无需空值分支。

---

## 缺陷二（折叠不一致）——修复 + 诊断

### 根因 2-a：tool 归属快照过期（对应"同一回合折叠不完整"）

summary 节点只在 `turn/end` / `assistant/message` 时立即物化（`turn-activity.ts:74-77` publication），`data-dsh-ta-tools` 是 `summary.toolCallIds` 的**一次性快照**（`summary-view.tsx:137`）。若 `tool/call` 在 turn/end **之后**到达（DSH 不禁止 turn 尾部追加事件，`turn-activity.ts:71-72` 注释已承认 late assistant/message 存在），tool-call 节点因默认 immediate 会渲染新行，但 summary 快照不含该 callId → `computeRowTargets` tool 分支（`projector.ts:106-115`）匹配不上 → **该 tool 行漏折叠，与已折叠行并存**。

**修复（`turn-activity.ts` publication）**：对 `tool/call`、`tool/result` 也返回 `immediate`。turn/end 前 summary 未物化（`buildViewNode` 返回 null）→ 无节点可发布，查无副作用；turn/end 后晚到事件则重新物化 summary、刷新 `data-dsh-ta-tools`，折叠回归。

### 根因 2-b：projector 投影到全局第一个 flow（对应"正常问答有的整个回合没折叠"）

`projector.ts` 的 `applyTurnCollapse`/`applyAll` 都用 `document.querySelector('[data-chat-flow]')` 取**文档第一个会话列**（`projector.ts:305/354`），`setSession/collectSummaries/describeRows` 全部沿这一列（`projector.ts:49/305/354`）。一旦宿主同时存在 ≥2 个 `[data-chat-flow]`（多会话/多频道同开），当前会话的折叠会被投影到错误列 → 表现为"正常回合没折叠"。

**修复（`plugin/src/client/projector.ts` + `summary-view.tsx`）**：

- `summary-view.tsx` 在 summary 行根渲染 **`data-dsh-ta-session={`${sessionId}`}`**（新增 `DATA_SESSION` 常量）。
- `applyTurnCollapse`：先定位该 turn 的 summary 行（遍历 `[data-dsh-ta-turn]`），`row.closest('[data-chat-flow]')` 得**其所列**，在该列上 `collectSummaries`/`describeRows`（找不到行才回退全局第一个列，向后兼容）。
- `applyAll`（reconcile）：遍历 `document.querySelectorAll('[data-chat-flow]')`，对每列分别用**列内 summary 行的 `data-dsh-ta-session`** 读取 store 决策（空列跳过）。多会话互不污染。

### 根因 2-c：`model-retry` 行不被折叠（留诊断确认，本轮不改）

`computeRowTargets` 只识别 `assistant-step` / `tool-call`（`projector.ts:88-116`），`model-retry` 行（`client.js:8669-8733`，锚点落活动区内）落入 default → 永不隐藏。只要某 turn 出现过 `llm/retry`，折叠后会残留。**归属需从 engine 取 retryId→turn 映射，成本较高**，且与"普通一问一答"场景吻合度低——**本轮先不修**，靠下面的诊断日志确认真机是否出现；若复现再补归属扩展。

---

## 诊断钩子（可留可去）

`projector.ts` 的 `applyAll` 加一段仅当 `localStorage['dsh.turn-collapse.debug']==='1'` 生效的 `console.info`：输出 flow 数量、每列内已折叠 turn 数、未能归属的行 key/kind 列表。真机复现"仍有的没折叠"时据此精确定位根因 2-c 或残余时序问题。

---

## 待修改文件

| 文件 | 改动 |
|---|---|
| `plugin/src/client/activity-state.ts` | 加 `lastFinalizeSeq`；`step/end`、`llm/retry` 不再推进 `lastActivitySeq` |
| `plugin/src/client/turn-activity.ts` | `buildViewNode` anchor clamp `min(lastActivitySeq, lastFinalizeSeq-0.1)`；publication 对 `tool/call`、`tool/result` 改 `immediate` |
| `plugin/src/client/summary-view.tsx` | 渲染 `data-dsh-ta-session` |
| `plugin/src/client/projector.ts` | `applyTurnCollapse` 从 summary 行定位所属列；`applyAll` 遍历所有 `[data-chat-flow]` 列、按列内 session 读 store；`DATA_SESSION` 常量；可选 debug 日志 |
| `plugin/test/activity-state.test.ts` | 新增：含 `step/end`（位于最后 `assistant/message` 后）/`llm/retry` 的序列，断言 `lastActivitySeq` < 最后 `assistant/message` seq、`lastFinalizeSeq` 正确 |
| `plugin/test/projector.test.ts` | 若 `SummaryRef` 增字段同步更新断言（保持向后兼容则不动） |

不改：`styles.ts`（分隔线保持低对比）、`shouldAutoCollapse` 白名单（仅 `completed`）。

---

## 验证

- `cd plugin && npm run typecheck && npm test`（新增回归用例全绿）。
- 手测：一问一答完成后 summary 出现在**最终答案上方**；连续多轮位置正确；折叠后展开再刷新状态恢复；开两个会话窗口互不串扰；同一回合 tool 行全部一起折/展。
- 若仍有残留回合不折叠：开 `localStorage['dsh.turn-collapse.debug']='1'` 跑一轮，用日志确认是 `model-retry` 残留还是其它未归属行，再针对性扩展。